## BUG-1120 · favorite-sentence-kind-downcast
- **报告**：2026-07-26（用户：）
- **真实性**：✅ 真 bug（展示层降维）。根因 `hibiki/lib/src/pages/implementations/collections_page.dart:1055`（修复前）——`_showItemDialog` / `_buildItem` 用 `isVideoSentence = item.source == kFavoriteSentenceSourceVideo` 把收藏句 `source` 的 book/video/audiobook/lyrics 4 值域降成「视频 vs 其它」bool（同页 ×7 处），audiobook/lyrics 句在列表来源前缀、长按弹窗图标/标签上被静默展示为书——而 `packages/hibiki_audio/lib/src/audiobook/favorite_sentence_repository.dart:5-8` 文档明言收藏夹页应按四细分值标注来源。注意：打开行为**不是** bug——audiobook/lyrics 的 bookKey 共享 `hoshi://book/` 身份，`_openBook` 是正确目的地（reader 内处理有声书/歌词模式），缺陷仅在展示层不可辨 + bool 非穷尽脆弱。
- **[x] ① 已修复** — `favorite_sentence_repository.dart` 新增 `enum SentenceSourceKind { book, video, audiobook, lyrics }`（`dbValue` 与 `kFavoriteSentenceSource*` 落库串逐字节一致 + `tryParse` 严格解析）与宽松解析 `sentenceSourceKindOf(String? raw)`（未知/null/空串回退 book，与 `FavoriteSentence.fromJson` 向后兼容默认一致；持久化 JSON `source` 字段仍存原字符串，wire 契约不动）；`collections_page.dart` `_CollectionItem` 加 `sourceKind` getter，`_showItemDialog`/`_buildItem` 的图标、来源前缀（video→nav_video / audiobook→section_audiobook / lyrics→lyrics_mode，全复用现有 i18n 键）、打开分支改为穷尽 switch。行 402/591/1039 的 `== kFavoriteSentenceSourceVideo` 判据语义为「是否视频」且正确，本轮不改（控制爆炸半径）。提交 `bbe0426b4`。
- **[x] ② 已加自动化测试** — `packages/hibiki_audio/test/audiobook/sentence_source_kind_test.dart`（枚举 dbValue/tryParse/宽松回退契约）+ `hibiki/test/pages/collections_page_source_kind_test.dart`（widget：四来源各 seed 一条，断言列表前缀与长按弹窗图标/按钮标签）；守卫 `hibiki/test/pages/sentence_favorites_todo047_guard_test.dart:120` 同步改断言 `SentenceSourceKind.video`。提交 `bbe0426b4`。
- **备注**：audiobook/lyrics 行新增来源前缀属可见 UI 变化，正是仓库文档声明的预期行为；统计口径（video vs 其余归阅读）零触碰。
