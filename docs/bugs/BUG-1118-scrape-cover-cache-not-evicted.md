## BUG-1118 · scrape-cover-cache-not-evicted
- **报告**：2026-07-26（用户：）
- **真实性**：✅ 真 bug —— 刮削链路三处封面落盘均不调 `evictLocalCoverCache`，违反 `MediaCoverService` 文档不变量（`hibiki/lib/src/media/media_cover_service.dart:28-30`「同路径覆盖写后必须双键驱逐解码缓存」）：①下载落盘 `hibiki/lib/src/media/video/scraper/poster_downloader.dart:83`（rename 后直接 return）；②sidecar 复制 `hibiki/lib/src/media/video/scraper/poster_scraper_service.dart:619`（`_copyLocalPoster` rename 后直接 return）；③合集分发复制 `poster_scraper_service.dart:381`（`applyCandidateToBooks` copy 后直接 updateCover）。封面文件名由 bookUid 1:1 派生恒为同一路径，覆盖写后裸 `FileImage` 键与 `resizedFileImage`（ResizeImage）键仍留在 ImageCache，UI 重建命中旧解码，用户看到旧封面。
- **[x] ① 已修复** — `086484ee8`：三处落盘点各在写盘完成后补 `await evictLocalCoverCache(path)`（双键驱逐，与 `video_import_dialog.dart:105` / `media_cover_service.dart:103` 既有范式一致）；改动文件 `poster_downloader.dart`（downloadPoster rename 后）、`poster_scraper_service.dart`（`_copyLocalPoster` rename 后 + `applyCandidateToBooks` 分发 copy 后）。
- **[x] ② 已加自动化测试** — `086484ee8`：`hibiki/test/media/video/scraper/poster_downloader_test.dart`（「二次下载覆盖同路径后双键驱逐」）、`hibiki/test/media/video/scraper/poster_scraper_service_test.dart`（「sidecar 覆盖已有封面后双键驱逐」+「applyCandidateToBooks 合集分发每成员均双键驱逐」）；断言 helper 抽取到共享 `hibiki/test/helpers/cover_cache_test_helpers.dart`（populate/evict 双键断言，源自 `media_cover_service_test.dart`）。
- **备注**：三个既有 scraper 测试文件（poster_downloader / poster_scraper_service / auto_scrape_service）补 `TestWidgetsFlutterBinding.ensureInitialized()`——evict 需要 PaintingBinding。不触碰 cover_meta.json 语义、videoCoverFileName 键派生、CoverOrigin 保护逻辑。
