## BUG-1269 · 关闭词典的快捷键/鼠标键在弹窗表面仍然无效
- **报告**：2026-07-31（用户：关闭词典快捷键鼠标按键还是没用）。[BUG-1071](BUG-1071-dismiss-dictionary-mouse-and-keyboard-fails.md) 的复诉。
- **真实性**：✅ 真 bug。BUG-1071 修的两处都在**宿主页面**一侧，而失效发生在**弹窗自己**那一侧——它把这一格写成了「已知边界」，但那正是用户的常态操作落点。
  - **鼠标键**：唯一的鼠标消费者是正文 WebView 的 `onPointerSeek`（`reader_hibiki/webview.part.dart:2246`），只覆盖「点在弹窗矩形**之外**的正文区」。点词后弹窗就贴在光标旁，按侧键时指针几乎必然落在**弹窗表面**上，事件被弹窗的原生 WebView 吃掉，正文 JS 根本收不到 → 全程无反应。
  - **键盘键**：BUG-1071 的 `_reclaimReaderFocusForTouchPopup` 只在**弹窗渲染那一刻**把 Flutter 焦点抢回正文。用户与弹窗交互一次（滚动看释义 / 点释义 / 点发音）OS 焦点就回到弹窗 WebView，之后按键必然再次失效 → 表现为「时灵时不灵」。
  - **为什么弹窗侧本来该有的桥没起作用**：`DictionaryPopupWebView` 早有转发桥，但被 `capturesDictionaryPopupNavigationKeys`（`base_source_page.dart:170` 基类恒 false）门控成**漫画页专属**——阅读器/视频页压根没装；且它的键表**硬编码** `ArrowLeft/ArrowRight/Escape` 三个值，既不含「关闭词典」的用户绑定，也不跟随改键。
- **[x] ① 已修复** —
  - 共享桥 `webViewKeyBridgeScript`（`hibiki/lib/src/focus/webview_key_bridge.dart`）一般化：判据从「裸 `event.key` 列表」升级为 **token 列表**（`[Ctrl+][Shift+][Alt+][Meta+]<键名>`，键名同时匹配 `e.key` 与 `e.code`）。`InputBinding.serialize()` 的键名（`Escape`/`KeyD`/`Space`）逐字就是 DOM `code`，故注册表绑定可**原样**当 token 用，零映射表。旧版「带任一修饰键就整体放行」的独立分支随之消失——那条分支正是「绑成 Ctrl+D 的动作在 WebView 持焦时永远收不到」的原因；裸表放行组合键现在是「不命中」的自然结果。
  - 同一座桥新增**鼠标通道**（`mouseButtons` + `installMouseListeners`）：命中回传 `Mouse<n>`，并压掉 `auxclick`（侧键历史导航）/ `contextmenu` 默认行为；左键与未绑定按钮原样留给弹窗（不抢查词/滚动）。
  - 键表改为按 `handlerName` 派生的 `window` 槽、**每次注入覆盖**，listener 仍只装一次。旧版键表冻结在首次注入的闭包里——热槽弹窗 WebView 跨查词长期存活，改键后换不掉表。
  - 🔴 **转发的是整份宿主 scope，不是点名几个动作**：弹窗持焦时断掉的是宿主的**整条输入通道**，「关闭词典」只是用户最先撞上的那一格——翻页 / 有声书控制 / 播放暂停在弹窗持焦时同样全部落空，那是同一个 bug 的其它未报症状。「哪些键要转发」的名单本身就是补丁。落地侧与之对称：把回传的动作喂进宿主**既有的、与键盘路径同一个**的分发入口（阅读器 `_executeShortcutAction`、漫画 `_resolveMangaKeyAction`→`inputActionForShortcut`、视频 BUG-924 守卫），那些入口本就带「弹窗可见」的分支（阅读器每个 case 自判 `isDictionaryShown`、漫画收 `dictionaryShown` 参数），所以**不需要第二套语义**，弹窗持焦与 Flutter 持焦天然同源。
  - 门控从「漫画专属 bool」换成作用域契约：`dictionaryPopupInputScope` + `dictionaryPopupForwardedActions` → `dictionaryPopupInputSpecFor` 实时导出 token 表；回传 token 经 `resolveDictionaryPopupInputToken` 走**与键盘路径同一个** `resolve*`。**两个弹窗宿主各加一份对称钩子**：`base_source_page.dart`（阅读器/漫画）与 `dictionary_page_mixin.dart`（视频/首页查词/独立查词页）——它们各自建一次 `DictionaryPopupLayer`，是既有的重复，本轮不重构，只保持契约一致。
  - 阅读器接 `readerDismissDict`；漫画接翻页 + `mangaDismissDict`（顺带修掉它「改键后弹窗持焦仍按老键位响应」的老毛病，`_handleNativeNavigationKey` 的三分支硬编码 switch 换成 `InputBinding.deserialize`）。
  - **视频页**（`video_hibiki_page.dart`）：转发**整份** `video` scope——它的 dismiss 语义就是「浮层可见时**任一**已映射的视频快捷键先关顶层浮层」（BUG-924 的 `guardVideoShortcutsWithPopupDismiss`），而那三条输入通道（键盘表 / 手柄 / 裸空格覆盖）全部走 Flutter 焦点，浮层持焦时一条都收不到，于是「视频里关不掉词典」原样复发。落地用 `_dismissTopVisiblePopup()`（只关顶层、保留隐藏热槽 BUG-092），与守卫同一执行体，不走基类的清整栈。
  - 🔴 **`dictionaryPopupInputSpecFor` 恒减去 `dictionaryPopup` scope 已占用的键/鼠标键**：切词条（`popupNextEntry`/`popupPrevEntry`）与制卡（`popupMineEntry`）是**弹窗内**动作，宿主不能抢。视频页转发整份 scope，撞键概率最高；放在数据层做减法，JS 侧不必再判 scope。这与守卫把制卡键**排在守卫之外**是同一条边界。
  - 🔴 **模态让位（本轮发现的自伤回归）**：popup.js 的「已制卡动作」面板用 capture 阶段监听 Escape，注释写明「面板自己吃掉 Esc，否则会把整个查词窗一起关了」。而本桥同为 capture、且在 `onLoadStop` 就注册，**比面板早得多** → 会抢走它的 Esc，用户想关面板却关掉整个查词窗。修法：popup.js 开/关面板维护 `window.__hoshiPopupModalDepth`（计数，面板可叠开），桥在 `deferToPopupModal` 下于**键盘与鼠标两侧**都让位（连 `preventDefault` 都不做，交给面板的是个未被处理的事件）。popup.js 三镜像（`assets/popup` / `assets/browser_extension/vendor` / `tools/browser-extension/vendor`）同步，逐字节一致。
  - 新文件 `hibiki/lib/src/pages/implementations/dictionary_popup_input_bridge.dart`（spec + 导出 + 解析 + 脚本生成）。
- **[x] ② 已加自动化测试** — `hibiki/test/focus/webview_key_bridge_behavior_test.dart` + `.js`：**行为级**守卫，Dart 生成生产脚本 → node 真执行 → 合成事件断言转发结果（与 BUG-1012 同款 harness，无 node 时显式 skip）。覆盖：Escape/侧键转发、左键与未绑定键放行、长按不重复、裸表放行组合键、`Ctrl+KeyD` 真拦得到、`e.code` 通道（换输入布局不失效）、**改键后旧键立即失效且 listener 不叠加**、输入框/IME 放行、空表不拦、**模态让位（含嵌套叠开、归零后恢复、且不波及未开该选项的老宿主）**、老宿主（阅读器空格桥）语义零变化。已做**两次变异实测**：把 `forwardRepeats` 翻 true、把 `deferToPopupModal` 翻 false，测试各自立刻转红（证明 node 真在跑，不是 skip 假绿）。
  另加 `hibiki/test/pages/dictionary_popup_input_spec_test.dart`：token 表导出（改键跟随）、**减去 `dictionaryPopup` scope 占用键**（键盘与鼠标两侧）、未装载返回空表、token→动作解析（键盘/鼠标取值域不相交）。注入面守卫更新在 `test/focus/webview_key_bridge_test.dart` 与 `test/pages/dictionary_popup_inline_html_memo_test.dart`。
- **验证状态**：全量 `flutter analyze`（含 test）0 issue；`test/focus` + `test/reader` + `test/pages` + `test/lookup` + `test/shortcuts` + `test/media/video` + `test/dictionary` + `test/build` 共 **7139** 测试，5 个失败经逐个核对全为 develop 既有红（扫的源文件本轮零改动，如 `vn_view_mode_three_state_guard` 的断言字面量在 `origin/develop` 上就是 0 次命中）。
- **同根因未覆盖的另一处**：阅读器**正文** WebView 的键桥（TODO-1078 `onSpaceKey`）是同一个病的另一个实例——正文 WebView 持焦时，也只有**裸空格**一个键被转发回宿主，其余 reader 快捷键同样落空。本轮没顺手扩到整份 scope：正文 WebView 里 `preventDefault` 的面要大得多（选中文字后的 Ctrl+C 复制、方向键滚动等浏览器原生编辑/选择行为），需要单独设计排除集并单独验证，混进本轮会让爆炸半径不可控。
- **备注**：阅读器 / 漫画 / 视频三页均已接入。app 外 / galgame 的全局查词浮窗是 Windows 原生裸 WebView2（`global_lookup_window.cpp`），不走本条路径，不在本次修复覆盖内。
