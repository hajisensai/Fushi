## BUG-717 · 查词全链路延迟半秒级
- **报告**：2026-07-10（用户：「优化一下查词速度、包括查词弹窗。别人速度是10ms级别的。我们甚至要半秒」）
- **真实性**：✅ 真 bug。沿真实代码路径全链审计（触发→FFI 引擎→弹窗渲染），500ms 不是单点而是热路径上多处结构性浪费叠加，五个根因（P1-P5）：
  - **P1 弹窗双推双渲染**：每次查词把结果推进弹窗 WebView 两遍——`didUpdateWidget`（`hibiki/lib/src/pages/implementations/dictionary_popup_webview.dart:565`）推第一次，`_showPopupWaitingForRender` 的 postFrame 无条件 `refreshCurrentResult()`（`hibiki/lib/src/pages/base_source_page.dart:406`、`dictionary_page_mixin.dart:632`）全量重推第二次；且第二推覆写 `__hibikiRenderToken` 使第一推的 `popupRendered` 被丢弃（`dictionary_popup_webview.dart:1012`）——内容可见时刻=第二遍完整渲染完成，整段渲染成本 ×2 串行（40-240ms）。无条件重推是 BUG-523 时代的防御，其真实根因（macOS 资产路径致 popup.html 未加载）已另行修复。
  - **P2 查词历史全量重写**：每次查词 `_persistDictionaryHistory`（`hibiki/lib/src/models/dictionary_repository.dart:266`）把整份历史（默认 10 条完整 DictionarySearchResult）在 UI isolate 同步 toJson 再整表 DELETE+INSERT。生产库实测单条 54-231KB、整表 ~1.36MB/次，序列化循环恰好卡在弹窗显示帧之前（10-100ms）。
  - **P3 词典字体每推重读盘**：`DictionaryFontCss._inlineFontDataUrl`（`hibiki/lib/src/reader/dictionary_font_css.dart:97`）无缓存，配置导入字体时每次推结果都同步 readAsBytesSync+base64Encode（≤8MiB/文件）并把 MB 级 data:URL 过桥——热槽 WebView 的 `<style>` 本就跨渲染持久（0-200ms，视字体配置）。
  - **P4 原生查询管线整跑两遍**：`searchDictionary` 在 `lookup()` 拿到结构化结果后又调 `lookupPopupJson()`（`hibiki/lib/src/models/app_model.dart:3065/3087`），C++ 把同一个词的完整管线（scan×文本变体×去屈折×hash 查询×排序×zstd 解压）从零再跑一遍；FFI 缓存命中分支和 load-more 每页也照跑（原生查询成本 ×2，10-60ms）。
  - **P5 渲染完成信号被尾批拖住**：多词条结果首词条同步渲染完即可见，但 `popupRendered`（撤盖板/翻可见的唯一信号）要等 `setTimeout(0)` 尾批把全部词条 build 完才发（`hibiki/assets/popup/popup.js` renderPopup 多词条路径，10-100ms）。
- **[x] ① 已修复** — 提交：`e07e529ad`（分支 `worktree-lookup-speed-opt`）
  - P1：`refreshCurrentResult()` 改为按 `_lastPushedResult`/`_lastRenderedResult`（对象身份）去重并返回 bool：已渲染完→false（不重推）、推送在途→true（等信号）、漏推→真正补推（BUG-523 的安全网语义保留）。两个宿主 postFrame 按返回值处理「渲染信号早于盖板架起」竞态：false 时立即走既有 rendered 路径撤盖板，不空等 1.8s failsafe。
  - P2：历史落库改 trailing debounce 300ms + 2s 封顶（连续查词只写一次）+ 逐条序列化 memo（`Expando`，10 条里通常只序列化新增 1 条）；`flushDictionaryHistoryNow()` 注册进 `ExitFlushRegistry`（桌面关窗/Android 退后台写穿），`clearDictionaryHistory` 先取消 pending 防旧快照复活，`loadFromDb` 重载前先 flush 保持「变更先于重载落库」旧语义。崩溃最坏丢 ≤2s 查词历史（非用户数据）。
  - P3：`_inlineFontDataUrl` 加 (path→mtime+size) 键静态缓存，existsSync+lengthSync 合并为单次 statSync；maxBytes 检查保持在缓存命中前（小上限调用不会误吃大文件缓存）。
  - P4：两处 `lookupPopupJson` 替换为 Dart 侧 `buildPopupJsonFromLookup(results: ffiResults, ...)`（`packages/hibiki_dictionary/lib/src/language/language.dart:491`，与 C++ `build_popup_json` 逐字段对齐、有 parity 测试）。同源生成还消除「entries 来自缓存、popupJson 来自新查询」的双源分叉；C++ builder 保留（Android :popup 进程 JNI 路径继续用）。
  - P5：popup.js 多词条路径首词条（build+局部 postProcessRuby+applyCustomCSS）就绪后立即发第一次 `popupRendered`（宿主撤盖板/翻可见），尾批完成后发第二次（同 token；Dart 全部消费方幂等，Windows global-lookup host 依赖第二发量全高）。窗口期 `_renderInProgress` 令 `updatePopupIncremental` 回退全量 renderPopup（换代取消尾批，杜绝重复卡片）。三镜像已同步（assets/browser_extension/vendor + tools/browser-extension/vendor）。
  - 已评估不改：阅读器高亮 await（换精确锚点矩形，5-30ms 值得）；`maxResults=200`（load-more 分页深度硬依赖，收敛需改缓存键结构）；remoteLookupEnabled 时远端 HTTP 先于本地缓存（默认关闭；改序变更远端优先语义，留待独立决策）；C++ 引擎微优化（freq/pitch 截断后富化、排序键预计算——P4 后收益减半，留作后续）。
- **[x] ② 已加自动化测试** — 提交：`e07e529ad`。弹窗推送去重与早到信号（test/pages/dictionary_popup_push_dedup_test.dart）、历史 debounce/flush/清空防复活/scroll memo 失效（test/models/dictionary_repository_test.dart）、字体缓存命中与 mtime 失效（test/reader/dictionary_font_css_test.dart）、popup.js 双发+`_renderInProgress` 三镜像源码扫描守卫（test/dictionary/popup_render_signal_guard_test.dart）、popupJson parity 补 deinflectionTrace 断言。既有 BUG-523 全套弹窗守卫、byte-parity、wheel、nav_icon、global_lookup_host_test.mjs 全绿。
- **备注**：Windows 离屏 itest 两级实测（debug 构建）：
  - 2 词条生成词典（`perf-after-01` vs 基线 `perf-base-01`）：renderPopup 2→1 次、全量注入 2→1 次（32+21ms→38ms）、persistHistory 移出弹窗帧前。
  - **真实词典组合**（大辞林第四版+三省堂国語第八版+Novel 频率+NHK 音调，`integration_test/lookup_latency_perf_itest.dart`，RunId `perf-real-01`）：**端到端（真实 DOM 点击→弹窗可见）冷 66ms、复查 40-47ms**；engine-only 整句探针（10 词条结果）首查 3-8ms、缓存命中 0ms——引擎段已到 Yomitan 的 10ms 量级。测量含 ~10-20ms 测试挟具开销（点击派发桥往返+8ms 轮询粒度），生产真实体感更低。
  - 剩余 40ms 构成=跨语言桥往返×5-6（onTap→Dart、selectText eval、onTextSelected→Dart、结果注入、popupRendered 回调、高亮 eval）+2-3 帧界+注入 payload 解析。
  - **第二轮（桥往返治理，①③ 已实施）**：
    - ① 触发段 3 跳并 1 跳：点词门控（chrome 可见性/highlightOnTap）做成 Dart 单写、JS 只读的镜像 `window.__hoshiTapGate`（setup 脚本带初始真值，`_syncTapGateJs` 在 `_toggleChrome`/`_setChromeVisible`/onSettingsChangedLive 三个翻转点刷新）；门控通过时 tap 在 JS 侧直接跑同一个 `selectText`（命中/空白/链接/同字 toggle 四分支与旧链逐一相同），镜像缺失回落旧 onTap 链。运行时证据：itest 探针 `tapGate={"gate":{"chrome":true,"lookup":true,"maxLen":400},"sel":true}`。守卫：`test/reader/tap_gate_mirror_guard_test.dart`。
    - ③ 注入 payload 拆分：`popup_settings_injection.dart` 拆 head/entries/tail 三段（合并输出对全局查词栈/剪贴板面板逐字节不变）；in-app 热槽对静态段（主题/字体/词典样式/自定义 CSS/开关/名单）串级比对、变了才重发，每次查词只发 entries+renderPopup，onLoadStop 重置基线。实测注入 eval 57/39ms→36/34/31/20ms（约 −40%）。守卫：`dictionary_popup_push_dedup_test.dart` BUG-712 ③ 组 + `dictionary_popup_webview_test.dart` 推送顺序守卫（更新为新契约）。
    - 端到端效果的诚实说明：同代码两次 itest 运行 e2e 相差 ±16ms（机器负载噪声），热 WebView2 实测单跳仅约 3-5ms（此前按上限 15ms 估计偏高），故 ① 的 ~5-10ms 收益在该挟具噪声内不可分辨；③ 的注入耗时下降可直接测得。两项均为结构性止损（少 2 跳、少数十 KB/次重复解析），零权衡、带回落安全网。
    - ② 高亮 eval 解耦**未实施**（绘制时序有可感变化：高亮从弹窗前变弹窗后 ~10ms，非零损，待用户拍板）。
  - **第三轮（② 高亮 eval 解耦，用户复诉「app 内查词还是慢，没有 app 外快，慢了 5-10 倍」后实施）**：
    - 根因复核：app 外覆盖窗的显示门控**全程在 WebView2 内 JS/CSS**（`global_lookup_host.js` 就地拦截 `popupRendered` 翻 `data-content-ready`，CSS 直接淡入，不出 WebView），而 app 内是 Flutter 弹窗（显示要 popupRendered JS→Dart→setState→重建往返）。差异主乘数=旧 `_highlightAndShowPopup` **先 `await` 高亮 eval（打在正在渲染 EPUB 的大 reader WebView）、`finally` 才 `showDeferredPopup`**——弹窗显示+弹窗 WebView 内容注入被死死串在繁忙大 reader WebView 之后；reader 忙于分页/滚动时每跳 stall 到数十 ms，叠加即数倍差。
    - 修复：`reader_hibiki/lookup.part.dart` `_highlightAndShowPopup` 改为**先用原始选区 rect 立即 `showDeferredPopup`**（弹窗 WebView 注入与高亮 eval 分属两个 WebView、并行不争用），高亮 eval 异步回来拿到精修词 bbox 再经 `reanchorTopPopup(rect, gen)` 重锚（当年搁置的「绘制时序/精确锚点」顾虑由重锚解决：弹窗不覆盖被查词 BUG-767，代次守卫 `activeLookupGeneration` + `DictionaryPopupController.reanchorEntry` 的「仍显示/仍在栈/rect 有变」三校验防迟到回调错位新查词弹窗、防抖动）。高亮副作用（正文高亮被查词）从弹窗前挪到弹窗后 ~一跳，与 app 外一致（app 外根本不在正文高亮）。
    - 诚实边界：这拆掉的是「被繁忙大 reader WebView 串行卡住」的主乘数；但 app 内弹窗仍是 Flutter 架构（reveal 要 popupRendered 往返），无法完全等于 app 外纯 WebView 门控的 ~10ms。要彻底对齐需把 in-app 弹窗 reveal 改成纯 WebView CSS 门控（大改，触 BUG-135/523/717/767 一片基础设施），留独立决策。
    - 测试：`test/pages/dictionary_popup_controller_test.dart` 加 `reanchorEntry` 四例（可见重锚+通知 / rect 未变不通知 / 隐身条目不动 / 已出栈条目 no-op）；既有 `base_source_page_nested_popup_flash_test.dart`（showDeferredPopup 显示门控）、`dictionary_popup_push_dedup_test.dart`（P1 去重）全绿。
